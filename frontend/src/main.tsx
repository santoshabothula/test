import React from 'react';import ReactDOM from 'react-dom/client';import {Provider} from 'react-redux';import {createBrowserRouter,Navigate,RouterProvider} from 'react-router-dom';import {CssBaseline,ThemeProvider,createTheme} from '@mui/material';import {store} from './app/store';import Shell from './components/Shell';import MetadataPage from './pages/MetadataPage';import RuntimePage from './pages/RuntimePage';const router=createBrowserRouter([{path:'/',element:<Shell/>,children:[{index:true,element:<Navigate to="/metadata"/>},{path:'metadata',element:<MetadataPage/>},{path:'runtime',element:<RuntimePage/>}]}]);ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><Provider store={store}><ThemeProvider theme={createTheme({palette:{primary:{main:'#0f6cbd'}},shape:{borderRadius:10}})}><CssBaseline/><RouterProvider router={router}/></ThemeProvider></Provider></React.StrictMode>);
